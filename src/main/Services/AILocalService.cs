namespace Lernassistent.src.main.services
{
    public class AILocalService
    {
        public string ProcessLocally(string extractedText)
        {
            // Add actual logic to process text with local AI
            return $"Processed locally: {extractedText}";
        }
    }
}
